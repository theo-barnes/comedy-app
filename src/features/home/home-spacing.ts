import { spacing } from '@/theme';

// Shared vertical rhythm for role-based Home screens.
export const HOME_SPACING = {
  sectionGap: spacing.sm,
  sectionBottom: spacing.sm,
  featuredTop: spacing.sm,
  featuredBottom: spacing.md,
  sectionHorizontalPadding: spacing.lg,
} as const;
